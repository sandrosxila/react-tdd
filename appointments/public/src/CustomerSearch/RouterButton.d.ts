import { FC } from 'react';
declare type Props = {
    id?: string;
    queryParams: object;
    pathname?: string;
    disabled?: boolean;
};
export declare const RouterButton: FC<Props>;
export {};
